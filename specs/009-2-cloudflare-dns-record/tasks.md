# Execution Graph (DAG): Cloudflare DNS Record (replace Route 53)

**Input**: Design documents from `/specs/009-2-cloudflare-dns-record/`
**Prerequisites**: `plan.md` (architecture), `spec.md` (requirements + AC)
**Estimate**: 4 implementation tasks + 3 verification gates

## Stage 1: Implementation

- [x] T001 [Stage 1: Script] Create `terraform/environments/dev/scripts/create-cloudflare-record.sh` with the full content from spec §1.2: `set -euo pipefail`; defensive `curl` install (`command -v curl || sudo dnf install -y curl`); constants `DOMAIN="demo.vijote.dev"`, `ZONE_NAME="vijote.dev"`, `CF_API="https://api.cloudflare.com/client/v4"`; step (0) read token via `aws ssm get-parameter --name "/sdd-k8s-platform/secrets/cloudflare-api-token" --with-decryption --query 'Parameter.Value' --output text` with fail-fast guard; step (1) poll ALB DNS from `ingress-nginx-controller` Service status (30×10s, same logic as the old script); step (2) `curl -s -H "Authorization: Bearer $CF_TOKEN" "$CF_API/zones?name=$ZONE_NAME&status=active"` → zone ID via `python3` JSON parse, fail fast if absent; step (3) GET existing CNAME (`?type=CNAME&name=$DOMAIN`) → `PUT` (update) or `POST` (create) with body `{"type":"CNAME","name":"$DOMAIN","content":"$ALB_DNS","proxied":false}`; final echo. Make it executable (`chmod +x`)
- [x] T002 [Stage 1: Script] Delete `terraform/environments/dev/scripts/create-route53-record.sh` (replaced by T001's script)
- [x] T003 [Stage 1: Terraform] In `terraform/environments/dev/main.tf`: (1) rename `null_resource.apply_route53_record` → `apply_cloudflare_record`, (2) update the header comment block (009 → 009-2: Cloudflare is the DNS authority for vijote.dev; the Route 53 record was invisible because the domain is not delegated to Route 53), (3) `triggers`: keep `domain` + `instance_id`, drop the `route53_ref` line, (4) SSM command: `base64encode(file("${path.module}/scripts/create-cloudflare-record.sh"))` (was `create-route53-record.sh`), (5) `--comment` → `"Create Cloudflare CNAME record for demo.vijote.dev (009-2)"` (56 chars — under the 100-char SSM cap), (6) keep `depends_on = [null_resource.apply_aws_ccm, module.cluster_plumbing]`, the SSM-agent wait loop, bootstrap-instance-id gate, and poll loop unchanged, (7) update the comment reference at ~line 495 (cycle-avoidance note) from `apply_route53_record` to `apply_cloudflare_record`
- [x] T004 [Stage 1: IAM] In `terraform/modules/cluster-plumbing/main.tf`: delete the `aws_iam_role_policy.node_route53` resource and its `# Inline policy:` comment block (009). No other resource references it. Do NOT touch `node_ssm_parameters` (it already covers the token path) or any other policy

## Stage 2: Verification (CI / user-managed — per constitution P5/P6, agent does NOT run)

- [x] T005 [Stage 2: Verify] AC-001/AC-002 static: `terraform fmt -check -recursive` + `terraform validate` + `terraform plan -detailed-exitcode` — plan must show ONLY `aws_iam_role_policy.node_route53` deletion + `null_resource.apply_route53_record` destroy + `null_resource.apply_cloudflare_record` create; zero changes to other resources
- [x] T006 [Stage 2: Verify] AC-003: via SSM on the control plane — the Cloudflare CNAME record exists with `content` = ALB DNS and `proxied` = `false` (curl the Cloudflare API with the token from SSM, parse with python3)
- [x] T007 [Stage 2: Verify] AC-004/AC-005/AC-006: (1) `kubectl get certificate demo-vijote-dev -n sdd-apps -o jsonpath='{.status.conditions[?(@.type=="Ready")].status}'` → `True`, (2) `kubectl get clusterissuer letsencrypt-prod -o jsonpath='{.status.conditions[?(@.type=="Ready")].status}'` → `True`, (3) `curl -sSf https://demo.vijote.dev/ -o /dev/null -w '%{http_code}\n'` → `200` AND `curl -sSf https://demo.vijote.dev/api -o /dev/null -w '%{http_code}\n'` → `200` (proves DNS + TLS + routing end-to-end; no nslookup per AGENTS.md)
